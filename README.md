# Deploy trên Render

```bash
# Đảm bảo các file Docker đã được commit
git add backend/Dockerfile backend/docker-compose.yml backend/.env.example
git commit -m "Add Docker configuration for Render"
git push origin main
```
